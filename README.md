-c option will color output green if OK red if failuare
-g "group name" will excute only that group
-lg print list of availbale groups
-n "test name" will run only that test
-r# repeat # times
learn how to use cpputest
